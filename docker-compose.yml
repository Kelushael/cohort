networks:
  axis:
    external: false

services:
  axis-mcp:
    build: .
    container_name: axis-mcp
    restart: unless-stopped
    networks:
      - axis
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
